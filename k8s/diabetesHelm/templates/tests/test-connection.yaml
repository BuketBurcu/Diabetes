apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "diabetesHelm.fullname" . }}-test-connection"
  labels:
    {{- include "diabetesHelm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "diabetesHelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
