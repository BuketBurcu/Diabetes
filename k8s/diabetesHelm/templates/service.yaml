apiVersion: v1
kind: Service
metadata:
  name: {{ include "diabetesHelm.fullname" . }}
  labels:
    {{- include "diabetesHelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diabetesHelm.selectorLabels" . | nindent 4 }}
